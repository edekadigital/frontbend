import { detectImageWidths } from '../src/detectImageWidths';

describe('detectImageWidths()', () => {
  test('is a Function', () => {
    expect(detectImageWidths).toBeInstanceOf(Function);
  });

  // TODO extend coverage
});
